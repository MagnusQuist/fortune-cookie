apiVersion: v1
kind: Service
metadata:
  labels:
    app: fortune-cookie-backend
  name: fortune-cookie-backend
spec:
  ports:
    - port: 5000
      protocol: TCP
      targetPort: 5000
  selector:
    app: fortune-cookie-backend
  type: ClusterIP
